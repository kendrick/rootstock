/**
 * The wordmark as rendered: lowercase, with no logo and no tagline. One
 * constant because the header and the document title are the same word, and a
 * second literal is how a rename lands half-applied, with the tab saying one
 * thing and the page another.
 */
export const WORDMARK = 'rootstock';
